{
  "name": "cascade-routing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Where should the next euro go? A measured cascade: rules, a small model, a large one, a human.",
  "scripts": {
    "start": "node src/serveur.ts",
    "mesurer": "node src/mesurer.ts",
    "optimiser": "node src/optimiser.ts",
    "test": "tsc --noEmit && node --test src/*.test.ts",
    "typage": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/node": "^24.0.0"
  }
}
